pl0编译器实现的语法与pl0.pdf所定义的不是完全一样的，大致有一下几个不同的地方：
1.连续定义常量和变量的时候，不同的常量和变量之间可以用";"隔开，但pl0.pdf中只允许用","隔开
2.由于上面的原因照成了另一个不同，我们先看下面的程序：
var a;
a:=1.
按照pdf中的语法这种写法是合理但是编译器却会报错，因为它将后面的a当作时另一个变量的定义了。对与常量是同样的情况。

应该还会有别的不同，暂时不是特别明晰。
不过先总结一下，从大体框架来看，其实都是差不多的，而从实现上来说，这些不同点可以方便程序的实现。这是我个人的理解。
