pl0编译器在确定出错信息以及源程序出现的位置时，它是通过变量cc来完成的。cc通过扫描所搜索的字符，就像一个scanner一样，当它将一个字符串读取结束之后，这个被读的字符串就将进入语法分析步骤，此时如果发现错误，错误位置就是cc所指向的前一个字符就是所分析字符串的最后一个字符，所以指向的地方位cc-1即可，这是错误的地方。这样就将错误和错误在源程序所发生的位置给联系起来了，至于错误信息是什么，则根据error中的n参数来确定，这样就像出错的信息和出错的位置一一对应了。
