1.pl0中间代码：
代码格式是由三段组成的: F  L  A
其中F是操作符，L是层次数，A是操作数(包含了常量，数据地址，程序地址，运算类别)
整个代码分为数据段和地址段
LIT        将常数置于栈顶
LOD        将变量值置于栈顶
STO        将栈顶的值赋予某变量
CAL        用于过程调用的指令
INT        在数据栈中分配存储空间
JMP，JPC   用以if,while语句的条件或无条件控制转移指令
OPR        一组算术或逻辑运算

2.gen():
这个函数的功能是形成中间代码，由一般的pl0代码，根据不同的代码意思来形成不同的中间代码。然后将其保存进code这个数组中。gen()中有三个参数x,y,z。其中x代表的是操作符，y代表的是层次数，z代表的是操作数(包含了常量，数据地址，程序地址，运算类别)。code这个数组就是用来保存每一条中间代码的，它就包含了每一条中间代码的F,L,A三个部分。cx是用来记录当前的代码段的长度的(实际上时cx-1)，并且它也指示了下一条即将生成了的目标指令的地址。也就时已经存储了多少中间代码了。cxmax代表了代码段所能允许的最大的代码长度是多少。

3.文法结构与中间代码：

赋值语句：
X := Y op Z
LOD Lev_Y Addr_Y
LOD Lev_Z Addr_Z
OPR       op
STO Lev_X Addr_X

if语句：
if C then S
C的目标代码
JPC    L1
S的目标代码
L1:...

while语句：
while C do S
L1:C的目标代码
JPC    L2
S的目标代码
JMP    L1
L2:...

call语句：
call A
CAL Lev_A Addr_A

dx数据所在数据段中的相对位置
lev代表了当前的层次数，比如当前过程，变量所在的相对层次数
levmax代表了可以到达的最大的层次数，
