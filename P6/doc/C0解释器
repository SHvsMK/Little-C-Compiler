C0生成的Pcode的解释器已经在ast中完成了，就是pl0中解释器的移植。但为了能够单独的运行.Pcode程序，所以单独的写了一个专门运行.Pcode程序的解释器，就是生成的代码重新读入code段中执行即可。在src文档中，为C0interpret.c生成的可执行文件在bin中，为C0interpret。运行.Pcode程序就直接在主目录下./bin/C0interpret 文件名即可(注意，需要给出完整的地址，如果文件不是在当前目录下)。
