自己设计出来的编译器肯定是要以自己的名字命名的。这里主要大致的讲解一下我设计出来的这个编译出所能实现的所有功能。
读者可以通过打开C0.y文件，一边看文法一边看我的说明，这样会更好的理解到我所讲的东西。
1.变量及函数的定义。我这里可以允许全局变量以及局部变量的定义。并且当全局变量与局部变量有相同的变量的时候，根据C中的情况来处理到底使用哪一个变量。
2.数组的定义。我在设计的过程中为了方便，将数组的定义开始设计为了array，也就是定义数组必须以array开头。数组的定义也可以是全局和局部的。而且支持多维数组的定义与使用。数组的寻址不仅可以是静态寻址，也可以是动态寻址，也就是可以实现这样的寻址：
a[f(1,2)][b+c+d*4][a[1][1][1]].这个可以通过test文档中的C0test22的例子来观看结果。到目前为止结果还是比较理想的。
3.函数包括void型空函数以及funcint型返回整型的函数。也是为了在不过多修改文法的基础上添加新的功能所以将返回整形的函数定义为以funcint开始的。并且所有的函数都支持传递参数。而只有funcint函数支持返回参数。这个在test文档中有很多例子，建议参考例子C0test20，这是一个我比较满意的地方，实现了quicksort。
4.支持的Stmt。在我的C0中可以支持的一些功能有，赋值操作，引用函数，if语句，while语句，for语句，return语句(根据是否有返回值来决定后面是否有参数)，这个可以通过test文档中的C0test16参考。
5.基本的四则运算以及%运算，计算功能强大准确。可通过test文档中的C0test23来参考。
以上就是我的C0编译器所能实现的功能的一个总汇，而具体的细节实现在同一文件下的设计文档中提到。
6.支持输入输出操作。具体例子见test文档中的C0test20程序。
7.还有非常少的一点错误判断
Made By SHvsMK
王一橙
PB11011030
