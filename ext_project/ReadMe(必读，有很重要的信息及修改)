这个文档包含了10个文件。
(读bin，非常重要)
bin：可执行文件所在位置
config：C0.lex C0.y
include : common.h util.h
doc：这里面包含了6个文件，包含了PCode设计文档，SHvsMK C0 compiler final design instruction, 扩展实验设计文档，之前C0存在的bug修改及记录，C0解释器，一个说明指示(这个很重要，建议先读)。
src：使用bison所生成的一些文件以及ast.c symtab.c 以及解释器生成函数C0interpret.c
test：C0的测试文件。(前面的有一些C0test是无法正确运行的，那些是为了检查文法设计而设计的，后面的一些C0test都是可以正确编译并且执行的。)
makefile：makefile
run.sh:自动运行脚本
C0.sh最终的高级运行脚本(主要用此脚本进行操作，可不用做其他的处理，自动生成所有内容，根据提示进行文件编译，执行，将编译和执行的文件放入test文件中即可，编译并执行的文件后缀名为.in，直接执行的文件后缀名为.Pcode)
README：本文件

编译完成之后使用方法：在当前目录下/bin/C0 <test/C0test1.in 或 ./bin/C0interpret test/wyc.Pcode(本文件名可以自己定义，即*.Pcode)
利用脚本完成方式：1.编译 ./C0.sh 根据提示选择1 C0test1
		 2.执行	./C0.sh 根据提示选择2 C0test1
		 3.编译执行 ./C0.sh 根据提示选择3 C0test1

Made By SHvsMK
PB11011030
王一橙
